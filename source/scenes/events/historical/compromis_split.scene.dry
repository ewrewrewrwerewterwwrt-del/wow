title: Compromís runs separately
view-if: year = 2019 and month = 2
tags: event
new-page: true
max-visits: 1
on-arrival: {!
    // Compromís leaves the Podemos coalition to field its own Valencia list (Apr 2019).
    // TODO: un-hardcode this / fold it into the podemos tree via potential fork
    Q.spa_compromis_active = true;
    window.spaSupportInject(Q, 'compromis', 'valencia', 6.5, 'podemos');
!}

- @post_event: TODO
